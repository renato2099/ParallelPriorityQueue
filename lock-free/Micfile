# Makefile for GNU make

LDLIBS = -mmic -pthread
CC = icpc
CFLAGS = -g -mmic -std=c++11 -Wall -pthread

OBJECTS = main.o LockFreeSkipList.o

main: $(OBJECTS) 
	$(CC) $(CFLAGS) -o $@ $(OBJECTS) $(LDLIBS)

%.o : %.cpp
	$(CC) $(CFLAGS) -c $<

clean:
	rm -f *.o main *~
